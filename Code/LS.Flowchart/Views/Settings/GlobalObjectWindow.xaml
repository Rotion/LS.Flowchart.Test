<Window
    x:Class="LS.Flowchart.Views.Settings.GlobalObjectWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LS.Flowchart.Views.Settings"
    xmlns:lsuc="clr-namespace:LS.WPF.Core.Library;assembly=LS.WPF.Core.Library"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="全局变量"
    Width="1400"
    Height="800"
    Background="Transparent"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    Topmost="True"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Border BorderBrush="{DynamicResource DefaultBackground}" BorderThickness="2" />
        <Grid Margin="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid
                x:Name="winBar"
                Grid.Row="0"
                Background="{StaticResource DefaultBackground}">

                <TextBlock
                    Margin="3,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="24"
                    FontWeight="Bold"
                    Foreground="{StaticResource DefaultForeground}"
                    Text="全局变量" />
                <lsuc:IconSmallButton
                    Width="40"
                    Height="40"
                    Margin="0,0,5,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Background="{StaticResource DefaultBackground}"
                    Command="{Binding CloseCommand}"
                    IconType="Close"
                    MouseOverBrush="{DynamicResource OrangeBrush}"
                    PathMargin="5" />
                <Border
                    Margin="0,2,0,0"
                    BorderBrush="{DynamicResource OrangeBrush}"
                    BorderThickness="0,2,0,0" />
            </Grid>
            <Grid Grid.Row="1" Background="WhiteSmoke">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50" />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <lsuc:CommonButton
                        Height="42"
                        Margin="10,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        BorderBrush="LightGray"
                        Command="{Binding OpCommand}"
                        CommandParameter="Add"
                        Content="添加变量"
                        CornerRadius="1" />

                    <DataGrid
                        Grid.Row="1"
                        MinWidth="600"
                        MinHeight="300"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        FontSize="20"
                        HeadersVisibility="All"
                        IsReadOnly="True"
                        ItemsSource="{Binding DataList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        VirtualizingStackPanel.IsVirtualizing="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn
                                Width="150"
                                Binding="{Binding Name}"
                                Header="变量名称"
                                TextBlock.TextAlignment="Center" />
                            <DataGridTextColumn
                                Width="150"
                                Binding="{Binding ObjectType}"
                                Header="变量类型" />
                            <DataGridTextColumn
                                Width="200"
                                Binding="{Binding DefaultValue}"
                                Header="默认值"
                                TextBlock.TextAlignment="Center" />
                            <DataGridTextColumn
                                Width="200"
                                Binding="{Binding Value}"
                                Header="当前值"
                                TextBlock.TextAlignment="Center" />
                            <DataGridTemplateColumn Width="*" Header="操作">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <StackPanel
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Orientation="Horizontal">
                                                <lsuc:CommonButton
                                                    Width="120"
                                                    Height="40"
                                                    Background="SkyBlue"
                                                    Command="{Binding Path=DataContext.EditCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGrid}}}"
                                                    CommandParameter="{Binding}"
                                                    Content="✎ 编辑"
                                                    Foreground="SkyBlue" />
                                                <lsuc:CommonButton
                                                    Width="120"
                                                    Height="40"
                                                    Margin="10,0,0,0"
                                                    Background="Red"
                                                    Command="{Binding Path=DataContext.DeleteCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGrid}}}"
                                                    CommandParameter="{Binding}"
                                                    Content="✖ 删除"
                                                    Foreground="Black" />
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
                <Grid
                    d:Visibility="Collapsed"
                    Background="WhiteSmoke"
                    Visibility="{Binding ViewVis}">
                    <Grid
                        Width="500"
                        Height="300"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="LightGray">
                        <Grid.Resources>
                            <Style x:Key="title" TargetType="TextBlock">
                                <Setter Property="Width" Value="150" />
                                <Setter Property="HorizontalAlignment" Value="Right" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="FontSize" Value="20" />
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter Property="TextAlignment" Value="Right" />
                            </Style>
                            <Style x:Key="input" TargetType="TextBox">
                                <Setter Property="Width" Value="300" />
                                <Setter Property="Height" Value="50" />
                                <Setter Property="Margin" Value="5,0,0,0" />
                                <Setter Property="HorizontalAlignment" Value="Left" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="FontSize" Value="20" />
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                            </Style>
                        </Grid.Resources>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                            <TextBlock Style="{StaticResource title}" Text="名称：" />
                            <TextBox
                                d:Text="变量名称"
                                Style="{StaticResource input}"
                                Text="{Binding Data.Name}" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                            <TextBlock Style="{StaticResource title}" Text="变量类型：" />
                            <lsuc:CommonCombox
                                Width="300"
                                Height="50"
                                Margin="5,0,0,0"
                                CornerRadius="1"
                                DisplayMemberPath="Name"
                                ItemsSource="{Binding ObjectTypes}"
                                SelectedItem="{Binding SelectType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                SelectedValue="{Binding Data.ObjectType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                SelectedValuePath="Value" />
                        </StackPanel>
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <TextBlock Style="{StaticResource title}" Text="默认值：" />
                            <TextBox
                                d:Text="默认值"
                                Style="{StaticResource input}"
                                Text="{Binding Data.DefaultValue}" />
                        </StackPanel>
                        <Grid Grid.Row="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <lsuc:CommonButton
                                Background="LightGray"
                                Command="{Binding CancelCommand}"
                                Content="取消"
                                FontSize="30" />
                            <lsuc:CommonButton
                                Grid.Column="1"
                                Command="{Binding SaveCommand}"
                                Content="保存"
                                FontSize="30" />
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>

    </Grid>
</Window>
